<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:MovieSearchForms"
             x:Class="MovieSearchForms.Pages.MainPage">
    <ContentPage.Content>
        <StackLayout Margin="20,20,40,20">
            <Label HorizontalOptions="Center" 
                Text="Enter words in movie title:">
            </Label>
            <SearchBar x:Name="SearchText" Placeholder="Enter a movie title" Text="{Binding titleSearch}"
           SearchCommand="{Binding SearchCommand}"/>
            <Label 
                x:Name="MovieLabel" 
                FontSize="Large" >
            </Label>
            <ActivityIndicator 
                Color="Blue" 
                IsRunning="False" 
                x:Name="SearchTitleProgressBar" >
            </ActivityIndicator>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>